# This folder holds the compiled Python worker binary.
# It is generated by running: worker/build_worker.sh (Mac/Linux) or worker/build_worker.bat (Windows)
# The binary is NOT committed to git — it is built fresh in CI for each platform.
